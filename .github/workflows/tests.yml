name: Tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    name: Python 3.12 on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
          - macos-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Linux AT-SPI build libraries
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            gcc libcairo2-dev pkg-config python3-dev \
            libgirepository-2.0-dev gir1.2-atspi-2.0

      - name: Install dependencies
        run: python -m pip install -r requirements.txt

      - name: Compile Python sources
        run: python -m compileall -q app developer lavocado_packaging scripts tests main.py developer_main.py

      - name: Validate Linux capture routing
        if: runner.os == 'Linux'
        run: python scripts/validate_linux_capture.py --self-check

      - name: Run unit tests
        run: python -m unittest discover -s tests -v
