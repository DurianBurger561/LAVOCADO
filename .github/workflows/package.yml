name: Package

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  package:
    name: Package ${{ matrix.edition }} for ${{ matrix.label }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            label: Windows
            edition: user
            spec: lavocado.spec
            dist: dist/LAVOCADO
            executable: dist/LAVOCADO/LAVOCADO.exe
            archive: LAVOCADO-Windows.zip
            artifact: LAVOCADO-Windows
          - os: windows-latest
            label: Windows
            edition: developer
            spec: lavocado-developer.spec
            dist: dist/LAVOCADO-Developer
            executable: dist/LAVOCADO-Developer/LAVOCADO-Developer.exe
            archive: LAVOCADO-Developer-Windows.zip
            artifact: LAVOCADO-Developer-Windows
          - os: macos-latest
            label: macOS
            edition: user
            spec: lavocado.spec
            dist: dist/LAVOCADO.app
            executable: dist/LAVOCADO.app/Contents/MacOS/LAVOCADO
            archive: LAVOCADO-macOS.zip
            artifact: LAVOCADO-macOS
          - os: macos-latest
            label: macOS
            edition: developer
            spec: lavocado-developer.spec
            dist: dist/LAVOCADO Developer.app
            executable: dist/LAVOCADO Developer.app/Contents/MacOS/LAVOCADO-Developer
            archive: LAVOCADO-Developer-macOS.zip
            artifact: LAVOCADO-Developer-macOS

    steps:
      - name: Check out repository
        uses: actions/checkout@v7

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: |
            requirements.txt
            requirements-build.txt
            requirements-developer.txt

      - name: Install user dependencies
        if: runner.os == 'macOS'
        run: python -m pip install -r requirements.txt -r requirements-build.txt

      - name: Install CPU PyTorch for Windows packages
        if: runner.os == 'Windows'
        run: python -m pip install "torch>=2.4,<3" torchvision --index-url https://download.pytorch.org/whl/cpu

      - name: Install Windows user dependencies with CPU PyTorch
        if: runner.os == 'Windows'
        run: python -m pip install -r requirements.txt -r requirements-build.txt

      - name: Install developer dependencies
        if: matrix.edition == 'developer'
        run: python -m pip install -r requirements-developer.txt

      - name: Run unit tests
        run: python -m unittest discover -s tests -v

      - name: Download and verify all bundled models
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: python scripts/download_models.py --model all

      - name: Load all required models offline
        env:
          HF_HUB_OFFLINE: "1"
        run: python scripts/verify_model_runtime.py

      - name: Build native application
        run: python -m PyInstaller --noconfirm --clean ${{ matrix.spec }}

      - name: Run frozen offline self-check
        shell: bash
        run: '"${{ matrix.executable }}" --self-check'

      - name: Verify bundled dashboard resources
        run: >-
          python -c "from pathlib import Path;
          expected={'index.html','styles.css','app.js'};
          found={path.name for path in Path('dist').rglob('*') if path.parent.name == 'web'};
          missing=expected-found;
          assert not missing, f'Missing dashboard resources: {sorted(missing)}'"

      - name: Verify all bundled model files and digests
        run: python scripts/verify_model_bundle.py "${{ matrix.dist }}"

      - name: Verify User bundle excludes Benchmark Lab
        if: matrix.edition == 'user'
        run: python scripts/verify_user_bundle.py "${{ matrix.dist }}"

      - name: Verify Developer bundle includes Benchmark Lab
        if: matrix.edition == 'developer'
        run: python scripts/verify_developer_bundle.py "${{ matrix.dist }}"

      - name: Archive Windows application
        if: runner.os == 'Windows'
        shell: pwsh
        run: Compress-Archive -Path "${{ matrix.dist }}" -DestinationPath ${{ matrix.archive }}

      - name: Archive macOS application
        if: runner.os == 'macOS'
        run: ditto -c -k --sequesterRsrc --keepParent "${{ matrix.dist }}" ${{ matrix.archive }}

      - name: Upload application
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact }}
          path: ${{ matrix.archive }}
          if-no-files-found: error
          retention-days: 7
